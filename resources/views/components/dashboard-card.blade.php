@props(['title', 'value'])

<div class="bg-white dark:bg-gray-800 p-6 rounded-lg shadow text-center">
    <h4 class="text-gray-700 dark:text-gray-300 text-md font-semibold">{{ $title }}</h4>
    <p class="text-3xl font-bold text-blue-600 mt-2">{{ $value }}</p>
</div>
